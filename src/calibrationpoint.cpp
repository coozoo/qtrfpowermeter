#include "calibrationpoint.h"

// This file is intentionally left empty.
// The CalibrationPoint is a plain struct and does not require a source file,
// but it is included in the project to satisfy the build system if the
// header is processed by moc.
